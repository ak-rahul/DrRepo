"""Read-only tools investigator agents can call.

Import directly from the submodule you need, same reasoning as
`src.collectors`/`src.agents`: keeping this file empty avoids cross-module
import coupling.
"""
