# Testing
pytest==9.1.1
pytest-cov==7.1.0
pytest-asyncio==1.4.0
pytest-mock==3.15.1
pytest-timeout==2.4.0

# Code Quality
black==26.5.1
flake8==7.3.0
isort==8.0.1
mypy==2.1.0

# Dependency auditing (developer-run; the app's own dependency_audit collector
# talks to OSV.dev directly and does not shell out to this tool)
pip-audit==2.10.1

# Pre-commit
pre-commit==4.6.0
